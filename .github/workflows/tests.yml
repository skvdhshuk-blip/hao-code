name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  php:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php-version: ['8.1', '8.2', '8.3', '8.4']

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          coverage: none
          tools: composer:v2

      - name: Validate Composer metadata
        run: composer validate --strict

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: Check PHP syntax
        run: composer run lint

      - name: Run full non-live test suite
        run: composer run test:full

      - name: Audit dependencies
        run: composer audit --format=plain

  windows-paths:
    name: Windows tool compatibility
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none
          tools: composer:v2

      - name: Validate Composer metadata
        run: composer validate --strict

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: Check test discovery
        run: composer run test:discover

      - name: Run focused Windows compatibility tests
        run: >-
          php -d memory_limit=-1 vendor/bin/phpunit
          tests/Unit/BaseToolTest.php
          tests/Unit/DiffGeneratorTest.php
          tests/Unit/GrepToolTest.php
          tests/Unit/GlobToolTest.php
          tests/Unit/Hitl/HitlWindowsCompatibilityTest.php
          tests/Unit/ProcessSupervisorTest.php
          tests/Unit/SensitivePathGuardTest.php
          tests/Unit/TestDiscoveryCheckTest.php
          --no-progress

      - name: Run focused hook working-directory tests
        run: >-
          php -d memory_limit=-1 vendor/bin/phpunit
          tests/Unit/HookExecutorTest.php
          --filter process_start_failure
          --no-progress
